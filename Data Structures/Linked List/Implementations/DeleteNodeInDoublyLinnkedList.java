class GfG
{
    // function returns the head of the linkedlist
    Node deleteNode(Node head,int x)
    {
	 if(head == null) return;
	 
	 if(x==1) return head.next;
	 int position =1;
	 Node headRef = head;
	 while(head != null)
	    if(position = x)
	    {
	        head
	    }
	 
    }
}
